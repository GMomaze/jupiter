'use strict';

module.exports = {
  async up(queryInterface, Sequelize) {
    await queryInterface.createTable('aircraft_sid_applicability', {
      id: {
        type: Sequelize.UUID,
        defaultValue: Sequelize.UUIDV4,
        primaryKey: true,
      },

      sid_id: {
        type: Sequelize.UUID,
        allowNull: false,
        references: {
          model: 'cessna_sids',
          key: 'id',
        },
        onDelete: 'CASCADE',
      },

      aircraft_model: {
        type: Sequelize.STRING, // e.g. C172, C182
        allowNull: false,
      },

      serial_from: {
        type: Sequelize.STRING,
        allowNull: true,
      },

      serial_to: {
        type: Sequelize.STRING,
        allowNull: true,
      },

      createdAt: {
        allowNull: false,
        type: Sequelize.DATE,
      },

      updatedAt: {
        allowNull: false,
        type: Sequelize.DATE,
      },
    });
  },

  async down(queryInterface) {
    await queryInterface.dropTable('aircraft_sid_applicability');
  },
};