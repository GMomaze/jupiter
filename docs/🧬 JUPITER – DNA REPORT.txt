before you fix anything ask for the files/DDL's you need to see to make a informed descision and then use FULL FILE. Not method.




Audit UI 302 — /audit route redirecting to login because request is unauthenticated in test.

Login success redirecting to /auth/login — Session not persisting after login, causing redirect loop.

audit_log permission issue — Test DB user (jupiter_test) lacks DELETE privilege on audit_log.

component_lifecycle deadlock — TRUNCATE in parallel test execution causing PostgreSQL deadlock.





Audit reason not persisting (NULL instead of expected reason)

CANNOT_QUARANTINE — Component UPDATE returning 0 rows due to invalid or contaminated test state.

FK teardown order invalid — Child tables (workpack_tasks) not deleted before parent tables (task_cards/aircraft).

Structural schema mismatch — rf_asset_type table missing expected name column and/or required seed rows.

Structural FK violations — Required parent records (manufacturer, asset_type, aircraft) missing during inserts/deletes.

Empty component_lifecycle.test.ts suite — File exists but contains no tests.

TASK_NOT_FOUND in immutability tests — Task not created/seeded before sign/lock operations in that suite’s setup.

INVALID_TASK on ISSUE — Workpack attempting to attach a task that does not exist or was not persisted.

WORKPACK_NOT_FOUND on ISSUE — Workpack record not created or lost before issuing transition.

Aircraft lifecycle FK failure — component_models_asset_type_fk violation due to missing rf_asset_type seed in that suite.


1️⃣ rf_asset_type.name does not exist
→ Schema mismatch in component_lifecycle test

2️⃣ PRE-REQUISITE FAILED: No aircraft found
→ Tests still relying on cross-suite ordering

3️⃣ NO_AIRCRAFT_AVAILABLE_FOR_TEST
→ Same root cause

4️⃣ Audit tests now failing because:

jupiter_app was allowed to delete logs

→ DB privilege enforcement not applied

5️⃣ Audit trigger test failing
→ Likely migration/trigger issue in test DB
