
2.2

Notes
- compliance_items.status = master item status
- aircraft_compliance.stored_status = aircraft-level DB status
- aircraft_compliance.computed_status = derived due status

2.4.1.

Note:
Compliance task creation failures are logged/skipped inside the current resilience catch. Accepted for now.


INSERT INTO compliance_items (item_type, code, title, status)
VALUES ('AD', 'TEST-AD-001', 'Test AD', 'ACTIVE')
RETURNING id;

2.5
Note:
Recurrence due recalculation deferred until recurrence fields are added to compliance_items.

3.2
Known limitation:
certification.licence_number is currently a placeholder / not a true licence field.

6.4
BACKLOG — Planner Unassigned Tasks UX

Issue:
Unassigned Tasks are global, not scoped per workpack.

Root cause:
Tasks filtered against any editable workpack aircraft instead of specific workpack.

Required fix:
Per-workpack task list or grouped by aircraft.

Status:
Known issue — not blocking core workflow